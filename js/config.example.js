/**
 * Configuration file EXAMPLE for Hybrid Roofing Forms Portal
 * 
 * SETUP INSTRUCTIONS:
 * 1. Copy this file and rename it to 'config.js' (remove .example)
 * 2. Update the username and password below with your desired credentials
 * 3. DO NOT commit config.js to version control (it's in .gitignore)
 */

var AppConfig = {
  // Login credentials - CHANGE THESE!
  credentials: {
    username: 'admin',
    password: 'hybrid2026'
  },
  
  // Session settings (can leave as-is)
  session: {
    storageKey: 'hybridRoofing_authToken',
    rememberKey: 'hybridRoofing_rememberDevice'
  }
};
